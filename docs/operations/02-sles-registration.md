# SLES Registration

SLES nodes require SUSEConnect registration before package installation from SUSE repositories.

Do not commit registration codes.

Use environment variables or local-only files that are excluded by `.gitignore`.
